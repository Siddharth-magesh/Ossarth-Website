---
const navItems = [
  { name: 'About', href: '#about' },
  { name: 'Download', href: '#download' },
  { name: 'Documentation', href: '#documentation' },
  { name: 'Customize', href: '#customize' },
];
---

<header class="fixed w-full bg-white/80 backdrop-blur-md z-50 border-b border-gray-100">
  <nav class="container mx-auto px-6 py-4">
    <div class="flex items-center justify-between">
      <a href="#" class="text-2xl font-bold text-dark">Ossarth</a>
      <div class="hidden md:flex space-x-8">
        {navItems.map(item => (
          <a href={item.href} class="text-gray-600 hover:text-dark transition-colors">
            {item.name}
          </a>
        ))}
      </div>
    </div>
  </nav>
</header>